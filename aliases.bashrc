alias date-to-identifier="date +'%Y%m%d%H%M%S'"
alias docker-prune="docker ps --quiet | xargs --no-run-if-empty docker kill; docker system prune --all --volumes --force"
alias git-root='cd `git root`'
alias xs="xscreensaver-command --lock"
