alias date-to-identifier="date +'%Y%m%d%H%M%S'"
alias docker-prune="docker ps --all --quiet | xargs --no-run-if-empty docker kill; docker system prune --all --volumes --force"
alias xs="xscreensaver-command --lock"
